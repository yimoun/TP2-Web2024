@model MessageModel
@{
    string textAction = "Ajouter";
    string formAction = "Create";

    ViewData["Title"] = textAction + "un message";
}

<div id="product-createedit-page" class="container-fluid">
    <div class="row">
        <div class="offset-md-1 offset-lg-2 col-md-10 col-lg-8">
            <div class="page-title">
                <h2>
                    <i class="fas fa-list"></i>
                    @textAction un message
                </h2>
            </div>
        </div>

        <div class="row">
            <div class="form-section">
                <form asp-action="@formAction">
                    <input type="hidden" asp-for="Id" /> @*Pas besoin de l'afficher il sera passé dans l'url*@
                    <div class="form-horizontal">
                        <hr />
                        <div class="form-group row">
                            <label asp-for="Contenu" class="col-form-label col-md-3 gauche"></label>
                            <div class="col-md-9 gauche">
                                <input asp-for="Contenu" class="form-control" />
                                <span asp-validation-for="Contenu" class="text-danger"></span>
                            </div>
                        </div>
                        <hr />
                    </div>
                    <div class="text-center">
                        <input type="submit" value="@textAction" class="btn btn-primary" />
                    </div>
                </form>
            </div>
        </div>

        <div class="row">
            <div class=" col-md-12 links">
                <a asp-action="List" asp-controller="Section">Retour à la liste des sections</a>
            </div>
        </div>

    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
    }
